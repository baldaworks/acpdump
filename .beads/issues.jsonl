{"_type":"issue","id":"acpdump-inn.2","title":"Monitor release workflow and verify publication","description":"**REQ**: REQ-REL-003, REQ-REL-004, REQ-REL-005\n**Design**: Section 3.3\n\n**Scope**:\n- Monitor the `omnidist-release` GitHub Actions run triggered by `v1.0.1`.\n- Verify GitHub Release `v1.0.1` is created with multi-platform binary assets.\n- Verify npm publication job outcome.\n\n**Done-when**: `gh release view v1.0.1` displays the published release and assets.\n**Complexity**: Small\n**Depends-on**: `acpdump-inn.1`\n**Verification**: `gh release view v1.0.1` and `gh run view`","status":"open","priority":2,"issue_type":"task","assignee":"prism/apply/reviewer","owner":"alexey.samoylov@gmail.com","created_at":"2026-09-01T09:20:43Z","created_by":"Alexey Samoylov","updated_at":"2026-09-01T09:39:07Z","labels":["prism"],"dependencies":[{"issue_id":"acpdump-inn.2","depends_on_id":"acpdump-inn","type":"parent-child","created_at":"2026-09-01T15:20:42Z","created_by":"Alexey Samoylov","metadata":"{}"},{"issue_id":"acpdump-inn.2","depends_on_id":"acpdump-inn.1","type":"blocks","created_at":"2026-09-01T15:20:47Z","created_by":"Alexey Samoylov","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"acpdump-inn.1","title":"Tag v1.0.1 and push to origin","description":"**REQ**: REQ-REL-001, REQ-REL-002, REQ-REL-003\n**Design**: Section 3.1, 3.2\n\n**Scope**:\n- Create annotated git tag `v1.0.1` on `HEAD`.\n- Push `v1.0.1` to remote `origin` (`git@github.com:baldaworks/acpdump.git`).\n\n**Done-when**: `git tag -l v1.0.1` exists; `git ls-remote origin refs/tags/v1.0.1` confirms tag is on remote.\n**Complexity**: Small\n**Verification**: `git ls-remote origin refs/tags/v1.0.1`","status":"closed","priority":2,"issue_type":"task","assignee":"prism/apply/reviewer","owner":"alexey.samoylov@gmail.com","created_at":"2026-09-01T09:20:35Z","created_by":"Alexey Samoylov","updated_at":"2026-09-01T09:21:54Z","closed_at":"2026-09-01T09:21:54Z","close_reason":"Tag created and pushed to origin","labels":["prism"],"dependencies":[{"issue_id":"acpdump-inn.1","depends_on_id":"acpdump-inn","type":"parent-child","created_at":"2026-09-01T15:20:35Z","created_by":"Alexey Samoylov","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"acpdump-inn","title":"Release and publish acpdump to GitHub Releases and npm","description":"## Pre-Authoring Analysis\n\nThe rebranding and repository migration of `acpdump` to `baldaworks/acpdump` is complete locally and on GitHub `main`. To make the new CLI available via `npm install -g @baldaworks/acpdump@latest`, `npx @baldaworks/acpdump@latest`, and GitHub Releases, a new release must be tagged and pushed to trigger the automated CI release workflow.\n\n# acpdump v1.0.1 Release and Publication — Requirements Document\n\n## 1. Overview\n\nCut release tag `v1.0.1`, push it to `baldaworks/acpdump`, trigger the GitHub Actions release workflow, and monitor the automated build, GitHub Release creation, and npm publication.\n\n**Actors**: Maintainer (Alexey Samoylov)\n\n## 2. Scope\n\n### In Scope\n- REQ-REL-001: Tag `v1.0.1` on current `main` commit.\n- REQ-REL-002: Push `v1.0.1` to remote `origin` (`baldaworks/acpdump`).\n- REQ-REL-003: Monitor GitHub Actions `omnidist-release` workflow execution.\n- REQ-REL-004: Verify GitHub Release `v1.0.1` is published with binary assets.\n- REQ-REL-005: Verify npm package `@baldaworks/acpdump@1.0.1` is published or monitor publish job.\n\n### Out of Scope\n- Changing source code or dependencies (already complete in previous story).\n- Unrelated feature work.\n\n## 3. Definitions and Glossary\n- **SemVer**: Semantic Versioning (`v1.0.1`).\n- **omnidist-release**: The automated CI release pipeline triggered on `v*` tags.\n- **Trusted Publishing**: OIDC-based npm publishing configured via GitHub Actions `id-token: write`.\n\n## 4. Requirements\n\n### Functional Requirements\n\n**REQ-REL-001** — Create Git Release Tag\nThe repository MUST have an annotated or lightweight tag `v1.0.1` pointing to the latest `main` commit.\n\n**REQ-REL-002** — Push Tag to Remote\nThe tag `v1.0.1` MUST be pushed to `origin` (`git@github.com:baldaworks/acpdump.git`).\n\n**REQ-REL-003** — CI Workflow Trigger\nPushing `v1.0.1` MUST trigger the `omnidist-release` GitHub Actions workflow.\n\n**REQ-REL-004** — GitHub Release Asset Verification\nThe GitHub release `v1.0.1` MUST exist and contain multi-platform assets (`darwin_amd64`, `darwin_arm64`, `linux_amd64`, `linux_arm64`, `windows_amd64`).\n\n**REQ-REL-005** — NPM Publication Verification\nThe GitHub Actions workflow MUST execute `npm publish` for `@baldaworks/acpdump`.\n\n### Non-Functional Requirements\nNone identified.\n\n### Constraints\n- **CON-001**: Tag MUST follow `v*` semver format matching the workflow trigger.\n\n## 5. Dependencies\n- **DEP-001**: GitHub Actions runners with repository access.\n- **DEP-002**: npm trusted publishing or npm credentials configured for `@baldaworks/acpdump` if required by registry.\n\n## 6. Assumptions\n- **ASM-001**: The maintainer has write access to push tags to `baldaworks/acpdump`. [KNOWN: verified by previous push]\n\n## 7. Risks\n\n| ID | Risk | Likelihood | Impact | Mitigation |\n|----|------|-----------|--------|------------|\n| RISK-001 | npm trusted publisher not configured for new repo | Medium | High | Inspect CI logs; if npm publish fails due to missing OIDC config, report actionable setup steps |\n\n## 8. Revision History\n\n| Version | Date | Author | Changes |\n|---------|------|--------|---------|\n| 1.0 | 2026-09-01 | Prism/Specify | Initial release requirements |","design":"# acpdump v1.0.1 Release and Publication — Design Document\n\n## 1. Overview and Requirements Summary\n\nThis design outlines the execution and verification of the `v1.0.1` release for `baldaworks/acpdump`.\n\n**Requirements covered**: REQ-REL-001, REQ-REL-002, REQ-REL-003, REQ-REL-004, REQ-REL-005, CON-001.\n\n## 2. Current Behavior \u0026 Evidence\n\n- The `main` branch is pushed to `baldaworks/acpdump`.\n- The GitHub Actions release workflow `.github/workflows/omnidist-release.yml` triggers on `tags: [\"v*\"]`.\n- The workflow has three jobs:\n  1. `prepare`: uses `omnidist --profile 'acpdump' build`, `stage`, `verify`, packages tarballs for 5 target platforms, and uploads artifacts.\n  2. `publish_github_release`: downloads assets and creates a GitHub Release with notes.\n  3. `publish_npm`: downloads staged npm artifacts and publishes `@baldaworks/acpdump` via `omnidist --profile 'acpdump' npm publish`.\n\n## 3. Proposed Execution Architecture\n\n### 3.1 Local Release Tagging (REQ-REL-001)\n- Create tag `v1.0.1` on the latest `main` commit:\n  `git tag -a v1.0.1 -m \"Release v1.0.1: initial acpdump rebrand under baldaworks org\"`\n\n### 3.2 Remote Tag Push (REQ-REL-002)\n- Push the tag to GitHub:\n  `git push origin v1.0.1`\n\n### 3.3 Pipeline Monitoring \u0026 Verification (REQ-REL-003, REQ-REL-004, REQ-REL-005)\n- Watch the GitHub Actions workflow run using `gh run watch`.\n- Verify GitHub Release assets with `gh release view v1.0.1`.\n- Verify npm publication job outcome.\n\n## 4. Verification Strategy\n\n- `git tag -l v1.0.1`\n- `gh run list --workflow=omnidist-release.yml`\n- `gh release view v1.0.1`\n- `npm view @baldaworks/acpdump@1.0.1 version` (if npm access is configured)\n","acceptance_criteria":"**REQ-REL-001**: `git tag -l v1.0.1` returns `v1.0.1`.\n**REQ-REL-002**: `git ls-remote --tags origin v1.0.1` returns the commit hash.\n**REQ-REL-003**: `gh run list --workflow=omnidist-release.yml` shows a running or completed run for tag `v1.0.1`.\n**REQ-REL-004**: `gh release view v1.0.1` shows the release with assets.\n**REQ-REL-005**: `gh run view \u003crun-id\u003e` shows the release jobs status.","status":"open","priority":2,"issue_type":"story","owner":"alexey.samoylov@gmail.com","created_at":"2026-09-01T09:20:01Z","created_by":"Alexey Samoylov","updated_at":"2026-09-01T09:20:30Z","labels":["human:approved","phase:story:apply","prism"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"acpdump-qt7.6","title":"Create baldaworks/acpdump GitHub repo, update remote, push, rename directory","description":"**REQ**: REQ-REPO-001, REQ-REPO-002, REQ-REPO-003\n**Design**: Section 3.7\n\n**Scope**:\n1. `bd rename-prefix acpdump-` — rebrand Beads issue prefix from `acp-dump-` to `acpdump-`\n2. `gh repo create baldaworks/acpdump --public --description \"Inspect any stdio ACP server command\"` (REQ-REPO-001)\n3. Commit all prior changes\n4. `git remote set-url origin git@github.com:baldaworks/acpdump.git` (REQ-REPO-002)\n5. `git push -u origin main --tags`\n6. `mv /home/bigboss/Projects/acp-dump /home/bigboss/Projects/acpdump` (REQ-REPO-003)\n\n**Done-when**: `gh repo view baldaworks/acpdump` succeeds; `git remote get-url origin` returns `git@github.com:baldaworks/acpdump.git`; project directory is `/home/bigboss/Projects/acpdump`; Beads issues use `acpdump-` prefix.\n**Complexity**: Small\n**Depends-on**: TASK-004 (acp-dump-qt7.4), TASK-005 (acp-dump-qt7.5) — all code changes must be complete\n**Risks**: `gh` CLI not authenticated for baldaworks org — mitigate by checking `gh auth status` first.\n**Verification**: `gh repo view baldaworks/acpdump`, `git remote get-url origin`, `bd list` shows `acpdump-` prefix\n**Rollback**: `gh repo delete baldaworks/acpdump --yes`, restore remote URL, `bd rename-prefix acp-dump-`","status":"closed","priority":2,"issue_type":"task","assignee":"prism/apply/reviewer","owner":"alexey.samoylov@gmail.com","created_at":"2026-09-01T08:55:41Z","created_by":"Alexey Samoylov","updated_at":"2026-09-01T09:17:02Z","closed_at":"2026-09-01T09:17:02Z","close_reason":"Implemented, independently reviewed, checks passed","labels":["prism"],"dependencies":[{"issue_id":"acpdump-qt7.6","depends_on_id":"acpdump-qt7.4","type":"blocks","created_at":"2026-09-01T14:55:45Z","created_by":"Alexey Samoylov","metadata":"{}"},{"issue_id":"acpdump-qt7.6","depends_on_id":"acpdump-qt7","type":"parent-child","created_at":"2026-09-01T14:55:41Z","created_by":"Alexey Samoylov","metadata":"{}"},{"issue_id":"acpdump-qt7.6","depends_on_id":"acpdump-qt7.5","type":"blocks","created_at":"2026-09-01T14:55:46Z","created_by":"Alexey Samoylov","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"acpdump-qt7.5","title":"Update README.md and AGENTS.md documentation","description":"**REQ**: REQ-DOC-001, REQ-DOC-002, CON-002\n**Design**: Section 3.6\n\n**Scope**:\n- `README.md`: replace all `acp-dump` → `acpdump`, all `github.com/normahq/acp-dump` → `github.com/baldaworks/acpdump`\n  - MUST preserve `@normahq/codex-acp-bridge` reference in examples unchanged (CON-002)\n- `AGENTS.md`: update binary name references, directory paths (`cmd/acpdump`), build command\n\n**Done-when**: README contains `acpdump` and `baldaworks`; `grep \"codex-acp-bridge\" README.md` still returns normahq reference; AGENTS.md references `cmd/acpdump`.\n**Complexity**: Small\n**Depends-on**: TASK-003 (acp-dump-qt7.3) — binary name must be settled\n**Risks**: Accidentally replacing codex-acp-bridge reference — mitigate with careful regex and CON-002 verification.\n**Verification**: `grep -c \"normahq\" README.md` returns exactly 1 (the codex-acp-bridge line).\n**Rollback**: `git checkout -- README.md AGENTS.md`","status":"closed","priority":2,"issue_type":"task","assignee":"prism/apply/reviewer","owner":"alexey.samoylov@gmail.com","created_at":"2026-09-01T08:55:16Z","created_by":"Alexey Samoylov","updated_at":"2026-09-01T09:15:21Z","closed_at":"2026-09-01T09:15:05Z","close_reason":"Implemented, independently reviewed, checks passed","labels":["prism"],"dependencies":[{"issue_id":"acpdump-qt7.5","depends_on_id":"acpdump-qt7.3","type":"blocks","created_at":"2026-09-01T14:55:39Z","created_by":"Alexey Samoylov","metadata":"{}"},{"issue_id":"acpdump-qt7.5","depends_on_id":"acpdump-qt7","type":"parent-child","created_at":"2026-09-01T14:55:16Z","created_by":"Alexey Samoylov","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"acpdump-qt7.4","title":"Rebrand omnidist config, npm packages, and CI workflows","description":"**REQ**: REQ-NPM-001, REQ-NPM-002, REQ-CI-001\n**Design**: Sections 3.4, 3.5\n\n**Scope**:\n- `.omnidist/omnidist.yaml`: profile key `acpdump`, tool name `acpdump`, main `./cmd/acpdump`, npm package `@baldaworks/acpdump`, repository-url `github.com/baldaworks/acpdump`\n- `git mv .omnidist/acp-dump .omnidist/acpdump`\n- Rename npm directories: `@normahq/acp-dump*` → `@baldaworks/acpdump*`\n- Update all `package.json` files: name, description, optionalDependencies, repository url, bin fields\n- Update all platform README.md files under `.omnidist/`\n- `.github/workflows/omnidist-release.yml`: profile `acpdump`, NPM_PACKAGE `@baldaworks/acpdump`, all artifact path references\n\n**Done-when**: `grep -c \"normahq\\|acp-dump\" .github/workflows/*.yml` returns 0; all package.json under `.omnidist/` reference `@baldaworks/acpdump*`; omnidist.yaml profile key is `acpdump`.\n**Complexity**: Medium (many files, mechanical changes)\n**Depends-on**: TASK-003 (acp-dump-qt7.3) — CLI binary name must be settled first\n**Risks**: Missed reference in deeply nested omnidist files — mitigate with recursive grep verification.\n**Verification**: `grep -rn \"normahq\\|acp-dump\" .omnidist/ .github/` returns only codex-acp-bridge examples.\n**Rollback**: `git checkout -- .omnidist/ .github/`","status":"closed","priority":2,"issue_type":"task","assignee":"prism/apply/reviewer","owner":"alexey.samoylov@gmail.com","created_at":"2026-09-01T08:55:00Z","created_by":"Alexey Samoylov","updated_at":"2026-09-01T09:15:21Z","closed_at":"2026-09-01T09:14:34Z","close_reason":"Implemented, independently reviewed, checks passed","labels":["prism"],"dependencies":[{"issue_id":"acpdump-qt7.4","depends_on_id":"acpdump-qt7.3","type":"blocks","created_at":"2026-09-01T14:55:14Z","created_by":"Alexey Samoylov","metadata":"{}"},{"issue_id":"acpdump-qt7.4","depends_on_id":"acpdump-qt7","type":"parent-child","created_at":"2026-09-01T14:54:59Z","created_by":"Alexey Samoylov","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"acpdump-qt7.3","title":"Rename Go module, imports, and CLI directory to baldaworks/acpdump","description":"**REQ**: REQ-GO-001, REQ-GO-002, REQ-CLI-001\n**Design**: Sections 3.2, 3.3\n\n**Scope**:\n- `go.mod` line 1: `module github.com/baldaworks/acpdump`\n- All .go files: `github.com/normahq/acp-dump/` → `github.com/baldaworks/acpdump/`\n- `git mv cmd/acp-dump cmd/acpdump`\n- `cmd/acpdump/cmd/command.go`: Cobra Use field → `acpdump`, example strings updated\n- `Taskfile.yaml`: BINARY_NAME → `acpdump`, MAIN_PATH → `./cmd/acpdump`\n- `go mod tidy`\n\n**Done-when**: `go build ./...` succeeds; `go test ./...` passes; `grep -r \"normahq/acp-dump\" --include=\"*.go\" .` returns empty; `ls cmd/acpdump/main.go` exists.\n**Complexity**: Small\n**Depends-on**: TASK-002 (acp-dump-qt7.2) — module path change must come after acpagent removal to avoid transient import conflicts\n**Risks**: None significant — mechanical find-and-replace.\n**Verification**: `go build ./...`, `go test ./...`\n**Rollback**: `git checkout -- . \u0026\u0026 git mv cmd/acpdump cmd/acp-dump`","status":"closed","priority":2,"issue_type":"task","assignee":"prism/apply/reviewer","owner":"alexey.samoylov@gmail.com","created_at":"2026-09-01T08:54:42Z","created_by":"Alexey Samoylov","updated_at":"2026-09-01T09:15:22Z","closed_at":"2026-09-01T09:13:48Z","close_reason":"Implemented, independently reviewed, checks passed","labels":["prism"],"dependencies":[{"issue_id":"acpdump-qt7.3","depends_on_id":"acpdump-qt7","type":"parent-child","created_at":"2026-09-01T14:54:42Z","created_by":"Alexey Samoylov","metadata":"{}"},{"issue_id":"acpdump-qt7.3","depends_on_id":"acpdump-qt7.2","type":"blocks","created_at":"2026-09-01T14:54:58Z","created_by":"Alexey Samoylov","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"acpdump-qt7.2","title":"Wire acpclient into inspect.go and remove go-adk-acpagent dependency","description":"**REQ**: REQ-GO-003, CON-001\n**Design**: Section 3.1\n\n**Scope**: \n- Update `internal/apps/acpdump/inspect.go`: replace `acpagent.NewClient`/`ClientConfig`/`SessionConfigValue` with `acpclient.New`/`Config`\n- Remove `acpSessionConfigValues` helper (no longer needed — `NewSession` takes just `cwd`)\n- Remove `github.com/normahq/go-adk-acpagent/v2` from `go.mod`\n- Run `go mod tidy`\n- Update test imports if needed (tests use internal mock subprocess, not acpagent)\n\n**Done-when**: `go build ./...` succeeds; `go test ./...` passes; `grep \"go-adk-acpagent\" go.mod` returns empty.\n**Complexity**: Small\n**Depends-on**: TASK-001 (acp-dump-qt7.1)\n**Risks**: Test helper subprocess protocol may need adjustment — mitigate by running tests.\n**Verification**: `go test ./...`\n**Rollback**: Revert inspect.go and go.mod changes","status":"closed","priority":2,"issue_type":"task","assignee":"prism/apply/reviewer","owner":"alexey.samoylov@gmail.com","created_at":"2026-09-01T08:54:26Z","created_by":"Alexey Samoylov","updated_at":"2026-09-01T09:15:22Z","closed_at":"2026-09-01T09:12:48Z","close_reason":"Implemented, independently reviewed, checks passed","labels":["prism"],"dependencies":[{"issue_id":"acpdump-qt7.2","depends_on_id":"acpdump-qt7","type":"parent-child","created_at":"2026-09-01T14:54:25Z","created_by":"Alexey Samoylov","metadata":"{}"},{"issue_id":"acpdump-qt7.2","depends_on_id":"acpdump-qt7.1","type":"blocks","created_at":"2026-09-01T14:54:40Z","created_by":"Alexey Samoylov","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"acpdump-qt7.1","title":"Implement internal/acpclient package with direct acp-go-sdk usage","description":"**REQ**: REQ-GO-003, CON-001\n**Design**: Section 3.1 — Drop go-adk-acpagent\n\n**Scope**: Create `internal/acpclient/client.go` with:\n- `Client` struct wrapping `*exec.Cmd`, `io.WriteCloser` (stdin), `*acp.ClientSideConnection`\n- `Config` struct: Command, WorkingDir, Stderr, Logger\n- `New(ctx, cfg)` — start subprocess, pipe stdin/stdout, create `acp.NewClientSideConnection` with a `noopClient`\n- `Initialize(ctx)` — call `conn.Initialize` with `InitializeRequest`\n- `NewSession(ctx, cwd)` — call `conn.NewSession` with empty `McpServers`\n- `Close()` — close stdin, wait with timeout, kill if needed\n\nImplement `noopClient` struct satisfying `acp.Client` interface: all methods return `MethodNotFound` except `SessionUpdate` which is a no-op.\n\n**CRITICAL — suppress SDK \"connection closed\" noise**: `coder/acp-go-sdk` `Connection.shutdownReceive()` (connection.go:490) unconditionally logs `\"connection closed\", \"cause\", \"peer connection closed\"` at `slog.LevelInfo` via the logger set with `conn.SetLogger()`. This is expected behavior for acpdump (subprocess exits after session/new). Fix: set a discarding slog logger (or one at LevelError) on the connection so this INFO-level noise is suppressed. Do NOT pass the app logger to `conn.SetLogger()` unless it filters INFO from the SDK.\n\n**Done-when**: `go build ./internal/acpclient/...` compiles; no \"connection closed\" noise on stderr during normal operation; unit test with mock subprocess passes Initialize+NewSession+Close lifecycle.\n**Complexity**: Medium\n**Risks**: `acp.Client` interface may have changed shape between SDK versions — mitigate by compiling against current `acp-go-sdk` version.\n**Verification**: `go build`, `go vet`, test confirms no stderr noise\n**Rollback**: Delete `internal/acpclient/`","status":"closed","priority":2,"issue_type":"task","assignee":"prism/apply/reviewer","owner":"alexey.samoylov@gmail.com","created_at":"2026-09-01T08:54:13Z","created_by":"Alexey Samoylov","updated_at":"2026-09-01T09:15:22Z","closed_at":"2026-09-01T09:12:02Z","close_reason":"Implemented, independently reviewed, checks passed","labels":["prism"],"dependencies":[{"issue_id":"acpdump-qt7.1","depends_on_id":"acpdump-qt7","type":"parent-child","created_at":"2026-09-01T14:54:12Z","created_by":"Alexey Samoylov","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"acpdump-qt7","title":"Move acp-dump to baldaworks GitHub org and rebrand to acpdump","description":"## Pre-Authoring Analysis\n\nThe project `acp-dump` is a small Go CLI under `github.com/normahq/acp-dump` that inspects stdio ACP servers. It is being moved to the `baldaworks` GitHub org with a rebrand to `acpdump` (no hyphen). The project has an unnecessary dependency on `go-adk-acpagent` which wraps `coder/acp-go-sdk` — only three symbols are used (`NewClient`, `ClientConfig`, `SessionConfigValue`) for subprocess management + Initialize/CreateSession. The SDK already provides `ClientSideConnection` with these methods, so the wrapper can be dropped.\n\n# acpdump Rebrand and Migration — Requirements Document\n\n## 1. Overview\n\nMove the `acp-dump` repository from `github.com/normahq/acp-dump` to `github.com/baldaworks/acpdump`, rebrand all references from `acp-dump`/`normahq` to `acpdump`/`baldaworks`, drop the `go-adk-acpagent` dependency in favor of direct `coder/acp-go-sdk` usage, and rename the local project directory.\n\n**Actors**: Repository maintainer (Alexey Samoylov)\n\n## 2. Scope\n\n### In Scope\n- REQ-REPO-001 through REQ-REPO-003: GitHub repo creation, remote update, directory rename\n- REQ-GO-001 through REQ-GO-003: Go module path, import paths, dependency replacement\n- REQ-CLI-001: Binary name change from `acp-dump` to `acpdump`\n- REQ-NPM-001 through REQ-NPM-002: npm scope and omnidist config rebrand\n- REQ-CI-001: GitHub Actions workflow updates\n- REQ-DOC-001 through REQ-DOC-002: README, AGENTS.md documentation updates\n\n### Out of Scope\n- Migrating `go-adk-acpagent` itself to baldaworks\n- Updating `@normahq/codex-acp-bridge` references in CLI examples (stays as-is per owner decision)\n- npm publish of the new `@baldaworks/acpdump` package (separate release cycle)\n- Archiving or redirecting the old `normahq/acp-dump` repo\n\n## 3. Definitions and Glossary\n- **ACP**: Agent Client Protocol — stdio-based protocol for AI agent communication\n- **omnidist**: Cross-platform Go binary distribution tool with npm packaging\n- **acpdump**: New project name (no hyphen), replaces `acp-dump`\n\n## 4. Requirements\n\n### Functional Requirements\n\n**REQ-REPO-001** — Create GitHub repository\nThe system MUST create a new public repository `baldaworks/acpdump` via `gh repo create`.\n\n**REQ-REPO-002** — Update git remote\nThe local git remote `origin` MUST be updated to `git@github.com:baldaworks/acpdump.git`.\n\n**REQ-REPO-003** — Rename local directory\nThe local project directory MUST be renamed from `acp-dump` to `acpdump`.\n\n**REQ-GO-001** — Go module path\nThe `go.mod` module path MUST change from `github.com/normahq/acp-dump` to `github.com/baldaworks/acpdump`.\n\n**REQ-GO-002** — Go import paths\nAll internal Go import paths MUST be updated from `github.com/normahq/acp-dump/...` to `github.com/baldaworks/acpdump/...`. Affected files: `cmd/acp-dump/main.go`, `cmd/acp-dump/cmd/command.go`, `internal/apps/acpdump/inspect.go`, `internal/apps/acpdump/inspect_test.go`.\n\n**REQ-GO-003** — Drop go-adk-acpagent dependency\nThe `github.com/normahq/go-adk-acpagent/v2` dependency MUST be removed. The three used symbols (`NewClient`, `ClientConfig`, `SessionConfigValue`) MUST be replaced with direct usage of `coder/acp-go-sdk` `ClientSideConnection` and subprocess management inline.\n\n**REQ-CLI-001** — Rename CLI binary\nThe `cmd/acp-dump/` directory MUST be renamed to `cmd/acpdump/`. The Cobra `Use` field, example strings, and `Taskfile.yaml` MUST reference `acpdump`. Binary output name MUST be `acpdump`.\n\n**REQ-NPM-001** — npm package scope\nThe omnidist npm package MUST change from `@normahq/acp-dump` to `@baldaworks/acpdump`. All platform-specific packages (`-linux-x64`, `-win32-x64`, `-darwin-arm64`, `-darwin-x64`, `-linux-arm64`) MUST follow the new naming.\n\n**REQ-NPM-002** — omnidist configuration\nThe `.omnidist/omnidist.yaml` profile MUST be updated: tool name → `acpdump`, main → `./cmd/acpdump`, npm package → `@baldaworks/acpdump`, repository-url → `github.com/baldaworks/acpdump`. The `.omnidist/acp-dump/` directory MUST be renamed to `.omnidist/acpdump/`.\n\n**REQ-CI-001** — GitHub Actions workflows\nThe `omnidist-release.yml` workflow MUST reference profile `acpdump`, npm package `@baldaworks/acpdump`, and use `acpdump` in all artifact paths and tar commands.\n\n**REQ-DOC-001** — README update\nThe `README.md` MUST replace all `acp-dump` with `acpdump`, all `normahq` with `baldaworks`, and update GitHub URLs. The `@normahq/codex-acp-bridge` reference in examples MUST remain unchanged.\n\n**REQ-DOC-002** — AGENTS.md update\nThe `AGENTS.md` MUST reflect the new binary name, directory paths, and build command.\n\n### Non-Functional Requirements\nNone identified — this is a pure rename/refactor with no behavioral changes to the CLI.\n\n### Constraints\n- **CON-001**: The CLI MUST remain functionally identical after rebrand — same output format, same flags, same exit codes.\n- **CON-002**: `@normahq/codex-acp-bridge` references in examples MUST NOT be changed.\n\n## 5. Dependencies\n- **DEP-001**: `gh` CLI must be installed and authenticated with permission to create repos in `baldaworks` org.\n- **DEP-002**: `coder/acp-go-sdk` must provide `ClientSideConnection` with `Initialize` and `NewSession` methods.\n\n## 6. Assumptions\n- **ASM-001**: The `baldaworks` GitHub org exists and the maintainer has admin access. [ASSUMPTION — verified by owner request]\n- **ASM-002**: The npm scope `@baldaworks` is available/registered on npmjs. [ASSUMPTION — not verified]\n\n## 7. Risks\n\n| ID | Risk | Likelihood | Impact | Mitigation |\n|----|------|-----------|--------|------------|\n| RISK-001 | npm scope `@baldaworks` not registered | Medium | High | Verify with `npm org ls baldaworks` before publish |\n| RISK-002 | `acp-go-sdk` client API insufficient for subprocess management | Low | High | Spike during Design to verify ClientSideConnection covers subprocess lifecycle |\n\n## 8. Revision History\n\n| Version | Date | Author | Changes |\n|---------|------|--------|---------|\n| 1.0 | 2026-09-01 | Prism/Specify | Initial requirements from owner request and clarification |","design":"# acpdump Rebrand and Migration — Design Document\n\n## 1. Overview and Requirements Summary\n\nThis design addresses the migration of `acp-dump` from `github.com/normahq/acp-dump`\nto `github.com/baldaworks/acpdump` with a full rebrand and dependency cleanup.\n\n**Requirements covered**: REQ-REPO-001, REQ-REPO-002, REQ-REPO-003, REQ-GO-001,\nREQ-GO-002, REQ-GO-003, REQ-CLI-001, REQ-NPM-001, REQ-NPM-002, REQ-CI-001,\nREQ-DOC-001, REQ-DOC-002, CON-001, CON-002.\n\n## 2. Current Behavior (Explorer Evidence)\n\n### Subprocess lifecycle\n\n`acpagent.NewClient` (client.go:130–203) performs:\n1. `exec.CommandContext` to start the ACP subprocess with `StdinPipe`/`StdoutPipe`\n2. Wire-logging wrappers around stdin/stdout (for debug mode)\n3. `acp.NewClientSideConnection(c, wireWriter, wireReader)` — the actual SDK call\n4. Background goroutines: `dispatchUpdates()` and `waitLoop()`\n\n`acpdump` only calls:\n- `client.Initialize(ctx)` → delegates to `conn.Initialize(ctx, InitializeRequest{...})`\n- `client.CreateSession(ctx, cwd, configValues, mcpServers)` → delegates to `conn.NewSession(ctx, req)` then applies config values\n- `client.Close()` → closes stdin, waits 200ms, kills process\n\n`acpdump` passes `nil` for `configValues` (via `acpSessionConfigValues(\"\")`) and\n`nil` for `mcpServers`, so `CreateSession` just calls `conn.NewSession` with an\nempty `[]acp.McpServer{}`.\n\n### Callback interface\n\n`acpagent.Client` implements `acp.Client` interface (callbacks). All callbacks\nexcept `SessionUpdate` return `MethodNotFound`. `SessionUpdate` only logs at trace\nlevel. Since `acpdump` never calls `Prompt`, no session updates are received.\n**All callbacks are dead code for this use case.**\n\n### Files touched by rebrand\n\n| Category | Files | Pattern |\n|----------|-------|---------|\n| Go module | `go.mod` | `github.com/normahq/acp-dump` → `github.com/baldaworks/acpdump` |\n| Go source | 4 files in `cmd/` and `internal/` | import path updates |\n| CLI entrypoint | `cmd/acp-dump/` | directory rename to `cmd/acpdump/` |\n| Taskfile | `Taskfile.yaml` | `BINARY_NAME`, `MAIN_PATH` |\n| Workflows | `.github/workflows/omnidist-release.yml` | profile, NPM_PACKAGE, artifact paths |\n| omnidist | `.omnidist/omnidist.yaml` + `.omnidist/acp-dump/` tree | profile key, npm config, dir rename |\n| Docs | `README.md`, `AGENTS.md` | name/org references |\n| Git | `.git/config` | remote origin URL |\n\n## 3. Proposed Architecture\n\n### 3.1 Drop `go-adk-acpagent` — Direct `acp-go-sdk` Usage (REQ-GO-003)\n\nReplace `acpagent.NewClient`/`Initialize`/`CreateSession`/`Close` with inline\nsubprocess management using `coder/acp-go-sdk` directly.\n\n**New internal package**: `internal/acpclient/client.go`\n\n```go\npackage acpclient\n\ntype Client struct {\n    cmd   *exec.Cmd\n    stdin io.WriteCloser\n    conn  *acp.ClientSideConnection\n}\n\ntype Config struct {\n    Command    []string\n    WorkingDir string\n    Stderr     io.Writer\n    Logger     *slog.Logger\n}\n\nfunc New(ctx context.Context, cfg Config) (*Client, error)\nfunc (c *Client) Initialize(ctx context.Context) (acp.InitializeResponse, error)\nfunc (c *Client) NewSession(ctx context.Context, cwd string) (acp.NewSessionResponse, error)\nfunc (c *Client) Close() error\n```\n\n**Key simplifications vs. acpagent.Client:**\n- No `dispatchUpdates` goroutine (no prompt streaming needed)\n- No `waitLoop` goroutine — just `cmd.Wait()` in `Close()`\n- No `activePrompt` tracking\n- No `PermissionHandler`\n- No wire-logging wrappers (existing zerolog debug logging is sufficient)\n- `acp.Client` callback interface: implement with a minimal no-op struct\n\nThe `acp.NewClientSideConnection` requires an `acp.Client` implementation. We\nprovide a `noopClient` struct that returns `MethodNotFound` for all methods.\nThis is exactly what acpagent does for all methods except `SessionUpdate` (which\nonly logs at trace level and is never triggered without `Prompt`).\n\n**Decision**: Inline the subprocess lifecycle (~60 lines) rather than keeping a\n500+ line dependency that includes prompt streaming, session resume, config option\nnegotiation, and terminal management.\n\n**Tradeoff**: Loses automatic wire-logging of JSON-RPC frames. Acceptable because\nacpdump's `--debug` flag already provides zerolog debug output, and the SDK's\nbuilt-in `SetLogger` provides JSON-RPC level logging.\n\n**Alternatives considered**:\n1. Keep `go-adk-acpagent` — rejected: unnecessary 500+ line dependency for 3 symbols\n2. Fork relevant portions — rejected: more maintenance burden than inline\n\n### 3.2 Module and Import Path Migration (REQ-GO-001, REQ-GO-002)\n\nMechanical find-and-replace:\n- `go.mod` line 1: `module github.com/baldaworks/acpdump`\n- Remove `github.com/normahq/go-adk-acpagent/v2` from require\n- All `.go` files: `github.com/normahq/acp-dump/` → `github.com/baldaworks/acpdump/`\n- Run `go mod tidy` to clean up\n\n### 3.3 CLI Binary Rename (REQ-CLI-001)\n\n- `cmd/acp-dump/` → `cmd/acpdump/` (directory rename)\n- `cmd/acpdump/cmd/command.go`: Cobra `Use` → `acpdump [--json] -- \u003cacp-server-cmd\u003e [args...]`\n- Example strings: `acp-dump` → `acpdump`\n- `Taskfile.yaml`: `BINARY_NAME: acpdump`, `MAIN_PATH: ./cmd/acpdump`\n\n### 3.4 npm/omnidist Rebrand (REQ-NPM-001, REQ-NPM-002)\n\n- `.omnidist/omnidist.yaml`: profile key `acpdump`, tool name `acpdump`,\n  main `./cmd/acpdump`, package `@baldaworks/acpdump`, repository-url updated\n- `.omnidist/acp-dump/` → `.omnidist/acpdump/`\n- All `npm/@normahq/acp-dump*` directories → `npm/@baldaworks/acpdump*`\n- All `package.json` files: name, description, optionalDependencies, repository url\n- All platform README.md files: references updated\n- Binary names in `bin` fields stay `acpdump` (already aligned after CLI rename)\n\n### 3.5 CI/CD Workflows (REQ-CI-001)\n\n- `omnidist-release.yml`: `--profile 'acpdump'`, `NPM_PACKAGE: '@baldaworks/acpdump'`\n- All `acp-dump` in tar/artifact paths → `acpdump`\n\n### 3.6 Documentation (REQ-DOC-001, REQ-DOC-002)\n\n- `README.md`: all `acp-dump` → `acpdump`, all `normahq` → `baldaworks` URLs\n  **except** `@normahq/codex-acp-bridge` in examples (CON-002)\n- `AGENTS.md`: paths, binary name, build command\n\n### 3.7 Repository Operations (REQ-REPO-001, REQ-REPO-002, REQ-REPO-003)\n\nExecution order:\n1. `gh repo create baldaworks/acpdump --public --description \"...\"` (REQ-REPO-001)\n2. All code changes committed\n3. `git remote set-url origin git@github.com:baldaworks/acpdump.git` (REQ-REPO-002)\n4. `git push -u origin main --tags`\n5. Rename local directory after push (REQ-REPO-003) — this is done outside git\n\n## 4. Data Flow\n\n```\nUser CLI invocation\n    → cmd/acpdump/main.go\n    → cmd/acpdump/cmd/command.go (Cobra)\n    → internal/apps/acpdump/inspect.go:Run()\n    → internal/acpclient/client.go:New()\n        → exec.Command → subprocess stdin/stdout pipes\n        → acp.NewClientSideConnection(noopClient, stdin, stdout)\n    → client.Initialize()\n        → conn.Initialize(ctx, InitializeRequest{...})\n    → client.NewSession()\n        → conn.NewSession(ctx, NewSessionRequest{cwd, mcpServers:[]})\n    → writeInspectHuman/writeInspectJSON → stdout\n    → client.Close()\n        → stdin.Close() → cmd.Wait()\n```\n\n## 5. Security and Trust Boundaries\n\n- No change to trust model. The CLI spawns a local subprocess and reads its stdout.\n- The `noopClient` callbacks reject all permission requests and file operations,\n  maintaining the same security posture as the current acpagent wrapper.\n- No network access, no credentials handling.\n\n## 6. Verification Strategy\n\n- `go build ./...` — compile check after all renames\n- `go test ./...` — existing tests use `TestACPDumpHelperProcess` as a mock\n  subprocess; tests verify Initialize, NewSession, error handling, and stderr\n  isolation. These tests are unaffected by the refactor since they test at the\n  `Run()` level.\n- `grep -r \"normahq/acp-dump\" . --include=\"*.go\"` — zero matches\n- `grep \"go-adk-acpagent\" go.mod` — zero matches\n- Manual: `go run ./cmd/acpdump -- opencode acp` for smoke test\n\n## 7. Risks and Open Questions\n\n| Risk | Mitigation |\n|------|------------|\n| `acp.NewClientSideConnection` requires background goroutine management | SDK handles this internally; `conn.Done()` channel available for coordination |\n| npm `@baldaworks` scope may not exist | Verify before npm publish (separate release cycle, out of scope) |\n\nNo blocking open questions remain.\n","acceptance_criteria":"**REQ-REPO-001**: `gh repo view baldaworks/acpdump` succeeds after creation.\n**REQ-REPO-002**: `git remote get-url origin` returns `git@github.com:baldaworks/acpdump.git`.\n**REQ-REPO-003**: Project directory is named `acpdump` at the same parent path.\n**REQ-GO-001**: `head -1 go.mod` shows `module github.com/baldaworks/acpdump`.\n**REQ-GO-002**: `grep -r \"normahq/acp-dump\" --include=\"*.go\" .` returns no matches.\n**REQ-GO-003**: `grep \"go-adk-acpagent\" go.mod` returns no matches; `go build ./...` succeeds; `go test ./...` passes.\n**REQ-CLI-001**: `ls cmd/acpdump/main.go` exists; `acpdump --help` shows `acpdump` in usage; Taskfile references `acpdump`.\n**REQ-NPM-001**: All `package.json` files under `.omnidist/` reference `@baldaworks/acpdump*`.\n**REQ-NPM-002**: `omnidist.yaml` profile key and tool name are `acpdump`; npm package is `@baldaworks/acpdump`.\n**REQ-CI-001**: `grep -c \"normahq\\|acp-dump\" .github/workflows/*.yml` returns 0.\n**REQ-DOC-001**: README contains `acpdump` and `baldaworks`; no stale `normahq` refs except `codex-acp-bridge` example.\n**REQ-DOC-002**: AGENTS.md references `cmd/acpdump` and `acpdump` binary.\n**CON-001**: `go test ./...` passes with identical behavior.\n**CON-002**: `grep \"codex-acp-bridge\" README.md` still returns the normahq reference.","status":"closed","priority":2,"issue_type":"story","owner":"alexey.samoylov@gmail.com","created_at":"2026-09-01T08:41:45Z","created_by":"Alexey Samoylov","updated_at":"2026-09-01T09:17:43Z","closed_at":"2026-09-01T09:17:43Z","close_reason":"Acceptance met","labels":["human:approved","phase:story:verify","prism"],"dependency_count":0,"dependent_count":0,"comment_count":0}
